<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.uconn.pha"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="4"
        android:targetSdkVersion="15" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Sherlock" >
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name" >
            <!-- android:theme="@android:style/Theme.Holo.NoActionBar" -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TabsActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Sherlock" />
        <activity
            android:name=".WellnessFormActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".WellnessReportActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".AlarmsActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".RxOtcActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".RxOtcFormActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".AllergiesActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ImageViewerActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar" />
    </application>

</manifest>